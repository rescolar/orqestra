# UI Mockups (HTML + CSS)

Archivos:

- `00_board.html`: vista principal con layout estable (header, columna izquierda, grid, panel derecho).
- `01_pendings-panel.html`: panel de pendientes con secciones por tipo.
- `02_add-participants-modal.html`: modal de alta por pegado de lista.
- `styles.css`: estilos compartidos.

Todos los datos son ficticios y están alineados con los documentos de `/docs/product`.
